# LADDER proxy — Python dependencies.
#
# SYSTEM PREREQUISITES (install before pip): the GSSAPI/Kerberos C libraries.
#   Debian/Ubuntu:  apt-get install -y libkrb5-dev krb5-user gcc
#   RHEL/Fedora:    dnf install -y krb5-devel krb5-workstation gcc
# httpx-gssapi builds on python-gssapi, which links against these.

fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
httpx-gssapi>=0.2
pydantic>=2
